/*java code*/
class Solution {
    public static void update(int a[], int n, int updates[], int k)
    {
        for(int i = 0 ; i < k ; i ++)
            for(int j = updates[i]-1 ; j < n ; j++) a[j]++;
    }
}
