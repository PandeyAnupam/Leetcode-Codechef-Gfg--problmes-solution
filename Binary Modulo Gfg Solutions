class Solution{
	int modulo(String s, int m) {
		//Write your code here
		int val = 0;
		for(int i=0;i<s.length();i++){
		    int digit = s.charAt(i) - '0';
		    val = (val * 2 + digit) % m; 
		}
		return val;
	}
}
