
class Solution {
    int[] kLargest(int[] arr, int n, int k) {
        // code here
        int res[] = new int[k];
     
         Arrays.sort(arr);
       
        for(int j =0, i=n-1; j<k;j++,i--)
        {
                res[j]=arr[i];
        }
       
        return res;
    }
}

